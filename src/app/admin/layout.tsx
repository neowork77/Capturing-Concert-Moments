export const metadata = {
  title: 'Admin — Photo Management',
  description: 'Manage gallery photos, edit metadata, and control visibility',
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
